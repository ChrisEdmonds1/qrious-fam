export const includesNumber = (array: number[], value: number) => {
  return array.indexOf(value) > -1
}

// export const orderBy = (key: string) => {
//   return (a:[], b:[]) => (a[key] > b[key]) ? 1 : ((b[key] > a[key]) ? -1 : 0)
// }